# Phase 1 手动测试检查清单

**测试日期**: 2025-11-16  
**测试范围**: 004-tool-management Phase 1 基础设置  
**测试环境**: Chrome Extension (Development Build)

---

## 前置条件

1. ✅ 开发服务器已启动 (`pnpm dev`)
2. ✅ Chrome 浏览器已安装扩展 (加载 `build/chrome-mv3-dev` 目录)
3. ✅ 扩展 ID 已记录: `chrome://extensions` 查看

---

## 测试用例

### TC-001: 选项页面访问测试

**步骤**:
1. 在 Chrome 地址栏输入: `chrome-extension://{EXTENSION_ID}/options.html`
2. 或右键点击扩展图标 → "选项" / "Options"

**预期结果**:
- ✅ 页面成功加载，无 404 错误
- ✅ 显示标题 "GitHub Switcher - 工具管理"
- ✅ 显示副标题（中英文双语）
- ✅ 无控制台错误（F12 检查）

**实际结果**: _____________

---

### TC-002: 工具列表渲染测试

**步骤**:
1. 打开选项页面
2. 检查工具列表区域

**预期结果**:
- ✅ 显示所有 9 个工具
- ✅ 每个工具包含：
  - 拖拽手柄 (⋮⋮)
  - 工具图标 (16x16)
  - 工具名称（英文）
  - 工具描述（40-60 字符英文）
  - 切换开关（默认全部开启）
- ✅ 工具顺序：
  1. GitHub.dev
  2. DeepWiki
  3. CodeWiki
  4. CodeSandbox
  5. StackBlitz
  6. nbviewer
  7. gitdiagram
  8. gitingest
  9. githistory

**实际结果**: _____________

---

### TC-003: CSS 样式测试

**步骤**:
1. 检查页面整体样式
2. 检查响应式布局

**预期结果**:
- ✅ 使用 `__github-switcher-` 前缀隔离样式
- ✅ 切换开关显示正确（圆形滑块动画）
- ✅ 拖拽手柄鼠标悬停时显示 `cursor: grab`
- ✅ 工具列表项有正确的间距和边框
- ✅ 预览区域显示占位符
- ✅ 重置按钮样式正确

**实际结果**: _____________

---

### TC-004: 切换开关交互测试

**步骤**:
1. 点击任意工具的切换开关
2. 观察行为

**预期结果**:
- ✅ 切换开关状态改变（开启 ↔ 关闭）
- ✅ 数据保存到 `chrome.storage.sync`
- ✅ 刷新页面后状态保持
- ✅ 禁用工具的切换开关可以再次开启

**实际结果**: _____________

---

### TC-005: 最后一个工具约束测试 (FR-011)

**步骤**:
1. 关闭所有工具，只保留一个开启
2. 尝试关闭最后一个工具

**预期结果**:
- ✅ 最后一个工具的切换开关应该被禁用（`disabled`）
- ✅ 鼠标悬停显示提示文本: "至少需要保留一个工具启用"
- ✅ 点击无效，无法关闭
- ✅ 控制台无错误

**实际结果**: _____________

---

### TC-006: 重置按钮测试

**步骤**:
1. 修改一些工具的开启状态
2. 点击 "重置为默认 / Reset to Defaults" 按钮
3. 在确认对话框中点击 "确定"

**预期结果**:
- ✅ 显示确认对话框（中英文双语）
- ✅ 点击确定后，所有工具恢复到开启状态
- ✅ 工具顺序恢复到默认 (1-9)
- ✅ 显示成功提示消息
- ✅ 刷新页面后状态保持

**实际结果**: _____________

---

### TC-007: 数据损坏警告测试

**步骤**:
1. 打开 Chrome DevTools → Application → Storage → Extension Storage
2. 修改 `enabledTools` 为无效数据: `[0, 10, "invalid"]`
3. 刷新选项页面

**预期结果**:
- ✅ 页面顶部显示黄色警告横幅
- ✅ 警告内容: "检测到数据损坏，已自动恢复到默认设置"（中英文）
- ✅ 点击关闭按钮 (✕) 可以隐藏警告
- ✅ 工具列表恢复到默认状态（全部开启）

**实际结果**: _____________

---

### TC-008: 性能测试 (NFR-002)

**步骤**:
1. 打开 Chrome DevTools → Performance
2. 刷新选项页面
3. 停止录制并分析

**预期结果**:
- ✅ 页面加载时间 ≤ 200ms
- ✅ DOM 构建时间 ≤ 100ms
- ✅ 无长任务 (Long Tasks)
- ✅ 无内存泄漏

**实际结果**: _____________

---

### TC-009: 无障碍访问测试

**步骤**:
1. 使用 Tab 键导航页面
2. 使用屏幕阅读器测试

**预期结果**:
- ✅ 所有交互元素可以通过键盘访问
- ✅ 切换开关有 `aria-label` 属性
- ✅ 警告横幅有 `role="alert"` 和 `aria-live="assertive"`
- ✅ 拖拽手柄有 `aria-label="拖拽以调整顺序"`

**实际结果**: _____________

---

### TC-010: 多标签页同步测试

**步骤**:
1. 打开两个选项页面标签页
2. 在标签页 A 中修改工具状态
3. 观察标签页 B

**预期结果**:
- ⚠️ **Phase 1 不支持**: 标签页 B 不会自动更新（需要手动刷新）
- ⚠️ 这是已知限制，将在 Phase 2 实现

**实际结果**: _____________

---

## 测试总结

**通过率**: _____ / 10  
**严重问题**: _____  
**一般问题**: _____  
**建议改进**: _____

**签名**: _____________  
**日期**: _____________
